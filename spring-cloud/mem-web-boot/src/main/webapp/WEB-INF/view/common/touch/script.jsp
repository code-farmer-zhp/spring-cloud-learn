<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<script src="${mStaticUrl}/assets/js/require.js?v=${version}"></script>
<script>
    var CSRF_TOKEN = '${CSRF_TOKEN}';
    var mUrl='${mUrl}';
    var mMyUrl='${mMyUrl}';
    var mHomeUrl='${mHomeUrl}';
    var mIUrl='${mIUrl}';
    var static_domain="${mStaticUrl}";
    var time_stamp="${version}";
    var ENV="${environmentVersion}";
    var BUY_DOMAIN='${mBuyUrl}';
    var mItemUrl='${mItemUrl}';
</script>